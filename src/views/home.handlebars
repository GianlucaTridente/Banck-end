<header>
  <h1>Products</h1>
</header>
<main>
  <div id="productsList">
    {{#each products}}
      <div class="product">
        <div class="product-img">
          <img
            src="{{thumbnails}}"
            alt="{{title}}"
            style="border-radius:40px; width: 200px;"
          />
        </div>
        <h2>TiTLE: {{title}}</h2>
        <h2>PRICE: ${{price}}</h2>
        <p>{{description}}</p>
      </div>
    {{/each}}
  </div>
</main>
<script src="/socket.io/socket.io.js"></script>

<script>
  const socket = io.connect('http:localhost:8080/',
</script>